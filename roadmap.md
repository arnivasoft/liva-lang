# Liva-Lang: Kod Kalitesi ve Yeni Özellik Yol Haritası

> Tarih: 2026-07-22
> Kapsam: `src/` (~48 dosya, ~47.9k satır), `stdlib/` (49 `.liva`, ~6.3k satır), araçlar (LSP/DAP/REPL/PM), testler, dokümantasyon.
> Yöntem: Üç paralel derin inceleme — kod kalitesi/teknik borç, dil özellikleri tamlık analizi, stdlib/ekosistem.

## Genel Değerlendirme

Proje genel olarak sağlıklı: `#if 0` ölü kod bloğu yok, TODO/FIXME birikimi yok, diagnostik sistemi X-macro tablosuyla (`DiagnosticKinds.def`, 149 kayıt) tutarlı, bellek yönetimi `unique_ptr` ağırlıklı ve temiz, CI matrisi (Windows/Linux/macOS + ASan/UBSan + coverage + benchmark regresyonu) güçlü.

Asıl riskler üç noktada toplanıyor:

1. **Birkaç devasa fonksiyon/dosya** (özellikle ~6.500 satırlık `IRGen::visitCallExpr`)
2. **Üç yerde elle senkronize edilen runtime ABI**
3. **"Parse ediliyor ama enforce edilmiyor" durumundaki sığ dil özellikleri** (pattern matching, lifetime'lar)

---

## 1. Kod Kalitesi / Teknik Borç

### 1.1 Kritik: Devasa fonksiyonlar

- `IRGen::visitCallExpr` (`src/IR/IRGenCall.cpp:71`) tek fonksiyon olarak **~6.500 satır** — dosyanın 8.026 satırının çoğu. İçinde 1.400+ `builder_->Create*` çağrısı ve ~66 satır-içi builtin isim karşılaştırması var. Kod tabanındaki en büyük bakım riski. Kategori bazlı handler'lara (builtin, method dispatch, static ctor, async, UI callback...) bölünmesi en yüksek getirili refactor.
- `TypeChecker::visitCallExpr` ~1.050 satır (`src/Sema/TypeChecker.cpp:2134-3188`).
- `IRGen::visitVarDecl` ~900 satır (`src/IR/IRGenDecl.cpp:835-1742`); `visitFuncDecl` ~580; `visitClassDecl` ~480.
- 2.000 satırı aşan 5 dosya: `IRGenCall.cpp` (8.026), `TypeChecker.cpp` (4.564), `LSPServer.cpp` (4.178), `IRGenDecl.cpp` (3.241), `IRGen.cpp` (2.125).

### 1.2 Kritik: Runtime ABI üç kopya halinde

Her native runtime fonksiyonu üç ayrı yerde elle tanımlanıyor:

1. LLVM imzaları — `src/IR/IRGen.cpp` (483 `getOrInsertFunction`)
2. JIT kayıtları — `src/JIT/JITEngine.cpp` (`REG(...)` makrosu, 244 adet)
3. C++ tanımları — `stdlib/runtime/runtime.cpp`

Senkronizasyon tamamen konvansiyona dayanıyor ("createRuntimeDecls unutulursa getOrPanic çöküyor" dersi bu yapının belirtisi). Diagnostiklerde zaten kullanılan `.def` X-macro deseni buraya uygulanırsa üçü tek kaynaktan üretilebilir.

### 1.3 Diğer bulgular

- **Kopya builtin isim listeleri** (ör. `logDebug/logInfo/...`) 4-5 yerde: `TypeChecker.cpp:149` ve `:2579`, `ModuleLoader.cpp:111`, `LSPServer.cpp:1238`, `IRGenCall.cpp:4276`.
- **Sessiz fallback'ler** tip çözümleme hatalarını diagnostik yerine yanlış kod üretimine dönüştürebiliyor (ör. `IRGenCall.cpp:6402` `elemTR = makeI32Type(); // fallback`, `IRGen.cpp:2067` "treat as opaque pointer"). `-fno-exceptions` + sıfır assert ortamında en olası "sessiz miscompile" kaynağı bunlar.
- **Uyarı ayarları**: `/W4` ve `-Wall -Wextra -Wpedantic` açık ama hiçbir toolchain'de warnings-as-errors yok (`cmake/CompilerFlags.cmake:10` açıkça `/WX-`); `-Wno-unused-parameter` / `-Wno-unused-variable` global kapalı. En azından CI'da tek toolchain'e `-Werror` önerilir.
- **Sessiz test atlamaları**: `tests/unit/IntegrationTest.cpp` içinde fixture dosyası bulunamazsa 10 test `GTEST_SKIP` oluyor — fixture kaybolursa entegrasyon kapsamı fark edilmeden sıfıra düşer; hard-fail yapılmalı.
- Bellek yönetimi temiz: yalnızca 27 `new`/`delete` metni, gerçek raw allocation'lar LLVM Module sahipliğinde; 297 smart-pointer kullanımı.

---

## 2. Dil Özellikleri

### 2.1 En büyük yapısal zayıflık: Pattern matching

`parseMatchExpr` yapısal pattern AST'si kurmuyor — token metnini `=>` görene kadar **string olarak birleştiriyor** (`src/Parser/ParseExpr.cpp:731-739`), IRGen bu string'i yeniden parse ediyor (`src/IR/IRGenCall.cpp:7820-7906`).

- **Çalışan**: `_` wildcard, `Enum.Case`, `Enum.Case(bindings)` (iç içe enum dahil), integer-literal pattern, subject binding + guard (`s if s >= 90`).
- **Eksik**: tuple/struct destructuring, string/float literal pattern, range pattern, or-pattern (`A | B`), `@` binding.

Yapısal bir Pattern AST'sine geçiş, dil sağlamlığı açısından en değerli tek yatırım.

### 2.2 Parse edilen ama enforce edilmeyen özellikler

- **Explicit lifetime'lar (`'a`)**: lexer/parser/elision var ama `LifetimeAnalysis.cpp` toplam 154 satır, sadece scope-derinliği kontrolü yapıyor; lifetime parametrelerini hiç okumuyor, `visitReturn` boş (`LifetimeAnalysis.cpp:47-49`). Şu an tamamen kozmetik.
- **GATs**: associated-type plumbing'i var ama lifetime enforce edilmediği için anlamlı well-formedness kontrolü yok.

### 2.3 Bilinen hatalar — kodda doğrulandı (kök nedenleriyle)

| Hata | Kök neden | Konum |
|------|-----------|-------|
| `let b = a` double-drop (çözüldü 2026-07) | `markMoved` yalnızca fonksiyon argümanlarında çağrılıyor; `VarDecl` init ve atamada move takibi yok | `OwnershipChecker.cpp:149-154`, `IRGenStmt.cpp:72-88` |
| Drop, Optional+if-let'te çalışmıyor (çözüldü 2026-07) | Koşullu temizlik sadece `heapOptionalStringVars` için var | `IRGenStmt.cpp:135-162` |
| `??` bazı bağlamlarda yanlış (çözüldü 2026-07) | Yalnızca bare identifier + MemberExpr chain doğru; diğer LHS'ler (çağrı sonucu, subscript) unwrap edilmeden aynen dönüyor | `IRGenExpr.cpp:302-352`, fallback `:350-351` — fix: `emitOptionalCoalesce` helper'a genel LHS + sağ-assoc + Sema `cloneTypeRepr` |
| `??` RHS tipi LHS'in unwrap edilmiş inner tipine karşı hiç kontrol edilmiyor (yeni bulundu, 2026-07, izlemede) | Sema `NilCoalesce` sonuç tipini yalnızca RHS'ten türetiyor, LHS inner tipiyle karşılaştırmıyor; tip uyuşmazlığı derleme-zamanı diagnostiğe değil opak LLVM PHI verifier çökmesine gidiyor — ör. `a ?? 5 == 5` öncelik nedeniyle RHS'i `bool`'a çeviriyor (LHS `i32?`) | `TypeChecker.cpp:2127` civarı (`visitBinaryExpr`, `NilCoalesce` dalı) — tüm `??` yolları için simetrik/önceden var olan açık |
| Generik monomorfize metodların `T?` döndürmesi hatalı codegen üretiyor (çözüldü 2026-07) | `monomorphize()`/`monomorphizeMethod()` visitFuncDecl'in kurduğu per-function dönüş/coroutine durumunu (`currentFuncOptionalInner_`, `currentIsAsync_`, `currentCoro*`) hiç kurmuyor/sıfırlamıyordu — `-> T?` sarma atlanıyor, üstelik Optional/async caller'ın bayat durumu mono gövdeye sızıyordu | `IRGenMono.cpp` — fix: her iki mono yolunda kaydet→sıfırla→(ikame sonrası) kur→geri yükle; 5 RuntimeExec testi |
| DynArray `pop()` void'du — eleman değeri hiç dönmüyordu (çözüldü 2026-07) | IRGen `.pop()`'u salt `len--` olarak üretiyordu (yerel `[T]` + struct-alan yolları); `return self.items.pop()` değer dönen fonksiyonda `ret void` üretiyordu. LSP (`pop() -> T`) ve DAP interpreter zaten değer-dönüşlü sayıyordu | `IRGenCallMethod.cpp` — fix: `emitDynArrayPopValue` (guard'lı son-eleman load + `len--` + boşta zeroinit PHI); Sema pop dönüş tipi = eleman tipi |
| Async generik fonksiyonlar (`async func foo<T>`) hiç desteklenmiyor ama temiz diagnostik yok (önceden var, 2026-07 izlemede) | Mono yolu her zaman düz sync fonksiyon üretir; `await work(x)` çağrısı Task handle beklerken sync imza bulup verifier hatasıyla düşüyor (final inceleme probe'u). Ucuz fix: Sema'da "async generic functions are not supported" diagnostiği | `IRGenMono.cpp` (bilinçli `currentIsAsync_ = false`), Sema diagnostik eksik |
| Argümansız generik statik metod `T`'yi çözemiyor, sessizce yanlış derleniyor (yeni bulundu, 2026-07, izlemede) | `Stack.new()` (parametresiz) — T çıkarımı yalnız argüman tiplerinden; argümansızda mono sessizce başarısız, program derleniyor ama exit 0 + hiç çıktı üretmeden bitiyor. `var s: Stack<i32> = ...` anotasyonu, `Stack<i32>.new()` ve `Stack.new<i32>()` de çıkarımı sürmüyor (hepsi sessiz yanlış) | COOKBOOK §6 bu yüzden `new(first: T)` tohumlu biçime alındı; kalıcı çözüm: dönüş-tipi/anotasyon tabanlı tip-argüman çıkarımı + çözülemezse diagnostik |
| `return self` from `mut self` | Codegen yolu yok | — |
| Integer widening kısıtlı | Yalnızca sabit-literal tarafında; genel coercion modeli yok | `IRGenExpr.cpp:234-243` |
| String `==` struct-wrapper'da | Primitive `string` için çalışıyor (`IRGenExpr.cpp:192-203`); sorun wrapper bağlamında tip tanıma. Kalıcı çözüm: protokol tabanlı `Equatable` | `IRGenExpr.cpp:167-181` |
| `Optional<Named>` yeniden atamada (`=`) bir sonraki okuma stale/kaymış değer verir (yeni bulundu, 2026-07, izlemede) | `while`/loop içinde aynı `Optional<NamedStruct>` değişkenine düz `=` ile yeniden atama; sıralı `let` ile üretilen ayrı değişkenlerde sorun yok | Task 2 raporu (`.superpowers/sdd/task-2-report.md`), minimal repro dahil |
| Fonksiyon çağrısında değer-tipi Drop struct'ı hem çağıran hem çağrılan taraf drop'luyor (önceden var, bu branch'ten bağımsız, izlemede) | Argüman geçişi move olarak işaretlenmiyor — `take(a)` sonrası hem `take` içindeki parametre hem de çağıran taraftaki `a` scope-exit'te drop çağırıyor (aynı payload iki kez) | `probe_r2.liva` (final inceleme), `IRGenCall.cpp` argüman geçiş kodegen'i — henüz kök neden dosya/satır olarak izole edilmedi |
| `??` unwrap kopyası Drop'lu tipte double-drop üretiyor (önceden var olan tek-sahip-kopya sınıfı, 2026-07 izlemede) | `let r = o ?? fb` (o: `Res?`, Drop'lu) — PHI sonucu izlenmeyen bir KOPYA; hem `fb`/kaynak hem `r` aynı payload'u drop'luyor (`DROP` ×2). Roadmap #4'ün "kalan" kısmıyla (kopya/move genellemesi) aynı fix sahası | Final inceleme probe'u: `mk(-1) ?? fb` → iki `DROP 99` |
| `a ?? nil` LLVM verifier çökmesi (önceden var, izlemede) | Literal `nil` RHS'in resolved tipi yok → yeni Optional-RHS Sema guard'ı atlanıyor; ucuz fix: `NilLiteralExpr` kind kontrolü | `TypeChecker.cpp` NilCoalesce dalı |
| Match expression'da bare binding-pattern + guard (`n if ... =>`) codegen hatası (önceden var, `??`'den bağımsız, izlemede) | "internal: undefined variable 'n' in code generation" — expression-pozisyonlu match'te guard'lı bare binding | Final inceleme probe'u (2026-07-24) |
| `\|\|` ve `&&` kısa-devre YAPMIYOR (çözüldü 2026-07) — CondBr+PHI kısa-devre lowering'i + parser'da çağrı-sonrası `\|\|` trailing-closure disambiguation'ı (`\|\|` yalnız `{` izliyorsa closure) | IRGen ikili mantıksal operatör (`\|\|`/`&&`) codegen'i short-circuit semantiği uygulamıyor, her iki operandı koşulsuz değerlendiriyor | IRGen binary logical op codegen; keşif: `cli::cli` Task 4, probe'la doğrulandı |
| Yalnız bir if/else dalında (veya bir döngünün yalnız bazı iterasyonlarında) bildirilen `[T]` DynArray local'i, alınmayan/atlanan koldaki scope-cleanup'ın ilklenmemiş/çöp storage'ı free etmesiyle heap corruption (`STATUS_HEAP_CORRUPTION`, sessiz çökme — stdout flush edilmeden abort) üretiyordu (çözüldü 2026-07 — zeroInitEntryAlloca: container alloca'sı entry block'ta {null,0,0} ile sıfırlanıyor, ulaşılmayan yollar free(NULL) no-op) | DynArray Drop/scope-exit codegen'i, çalışmayan koldaki (veya atlanan iterasyondaki) `let`/`var: [T]` local'i için de temizleme kodu üretip başlatılmamış pointer'ı "free" ediyor | Keşif: `cli::cli` Task 4; workaround: değişkeni fonksiyon başında bir kez bildir, dallarda/döngüde yalnız yeniden ata |
| Bool olmayan operandlarla `&&`/`\|\|` temiz diagnostik yerine LLVM verifier hatasına gidiyor (önceden var, 2026-07 izlemede) | Kısa-devre lowering'i yalnız i1 LHS'te devreye girer; bool-olmayan operandlar (ör. `if let` dışında Optional değişkenle `o && flag`) legacy eager-bitwise yola düşüp `and %Optional, i1` verifier hatası üretir | Ucuz fix: Sema'da mantıksal operatörlere bool-operand kontrolü; `IRGenExpr.cpp` non-i1 fallback |
| Top-level `var x = 0` (global değişken) livac'ı SEGFAULT ettiriyordu (çözüldü 2026-07) | Kök neden: IRGen visitVarDecl null insert-block dereferansı. Fix: Sema'da err_global_var_unsupported diagnostiği (const-olmayan top-level VarDecl reddedilir; `const` destekli kalır) + IRGen savunma katmanı | `TypeChecker.cpp` TU döngüsü, `IRGenDecl.cpp` visitVarDecl guard; 4 pinning testi |
| Çağrı sonucu üzerinde doğrudan member zinciri (`strSplit(s, ",").length`, `m.keys().length`) "cannot resolve member" hatası veriyor (önceden var, izlemede) | Member çözümü yalnız değişken-tabanlı kayıtlardan (varDynArrayTypes vb.) besleniyor; çağrı-sonucu geçici değerler için resolvedType tabanlı yol yok | Workaround: ara `let` binding'i (anotasyonlu); LANGUAGE-REFERENCE notu mevcut |
| Untyped `func main()` içinde bir if dalındaki çıplak `return`, LLVM verifier hatası veriyor (`ret void` vs `i32` — "Function return type does not match operand type") (yeni bulundu, 2026-07, izlemede — final inceleme probe'uyla doğrulandı) | main'in örtük `i32` dönüş tipi ile gövdedeki değersiz `return`'ün `ret void` üretmesi çelişiyor; ya çıplak `return` main'de `ret i32 0`'a çevrilmeli ya da Sema diagnostiği eklenmeli | Keşif: cli::cli Task 5 cookbook doğrulaması; minimal repro: `func main() { if x == 1 { return } }` |
| Struct alanlarında `pub` token'ı parse edilmiyor (`pub var x` alan düzeyinde sözdizimi hatası veriyor; alanlar zaten `pub struct` altında dışarıdan erişilebilir olduğu için pratik etki kozmetik) — ya parser'a alan-düzeyi `pub` desteği eklenmeli ya da bu davranış dokümante edilmeli (yeni bulundu, 2026-07, izlemede) | Struct alan-döngüsü yalnızca `var`/`let` token'ını tüketiyor, `pub`'ı hiç kontrol etmiyor | `ParseDecl.cpp:386-391` |
| Map fonksiyon parametresi/dönüşü/struct alanı desteklenmiyor (izlemede) | Doğrulanmadı — henüz kök neden izole edilmedi | — |
| Aynı-süreç "CompilerInstance state kirliliği" şüphesi ve tüm Cli flake'leri (çözüldü 2026-07 — kök neden state değil, [string] eleman-depolama UAF'ıydı) | Gerçek kök neden: dizi slotuna string depolarken sahiplik kopyası alınmaması (yerel element-atama + üye push) — serbest bırakılan blok geri dönüşünce eleman "true" okunuyordu; taze-süreçte ~%5, aynı-süreçte ~%10. Fix: dizi, string elemanlarının KOPYASINA sahip (liva_str_dup, tüm depolama yolları); ayrıca üye-alan element-ataması sessiz no-op'tu, gerçek store eklendi. `SelfHostTest.StrSplit*` flake'i ayrı izlenmeli (bağlantı kanıtlanmadı) | `IRGenCall.cpp` visitAssignExpr, `IRGenCallMethod.cpp` mpush; doğrulama: probe 0/150 taze süreç, ctest tek-test 0/100 (önce 6/100), examples tek-süreç 30/30 |

| `-Werror` kapsam boşlukları (2026-07 final incelemesi, izlemede) | (1) `liva_runtime`/`liva_ui`/fuzz hedefleri `liva_set_compiler_flags` çağırmıyor — LIVA_WERROR açıkken bile uyarı bayrağı/gate almıyorlar; (2) CI koşucularında LLVM olmadığından `liva_irgen`/`codegen`/`jit`/`livac` CI'da derlenmiyor — IRGen uyarı kapısı fiilen yalnız yerel clang-cl/MinGW; (3) GNU dalındaki `-Wno-unused-variable/parameter` bastırmaları CI'da unused sınıfını yakalamıyor (yalnız yerel clang-cl /W4 yakalar) | `CMakeLists.txt:134,174,182`, `tests/CMakeLists.txt:212`, `cmake/CompilerFlags.cmake` — takip işi: kapsamı genişlet |

| Modül-içi Sema hataları "module 'X' not found" olarak maskeleniyordu (çözüldü 2026-07) | Fix: parse + Sema maskeleme siteleri err_module_error ile modülün gerçek hatalarını (dosya:satır gömülü) import noktasına iletiyor; not-found yalnız gerçek dosya-yokluğunda | `ModuleLoader.cpp` loadModule; 2 SemaTest + e2e livac build doğrulaması |

| İç içe dizi (`[[i32]]`) push'u heap corruption ile çöküyordu (çözüldü 2026-07 — `[[T]]` çekirdek desteği: literal/length/index/push/for-in/index-atama; eleman = inline 24B DynArray struct; for-in değişkeni borrow) | İç dizi elemanı `ptr` olarak lower ediliyordu ama 24-baytlık `%DynArray` struct'ı 8-baytlık slota (elemSize 8) store ediliyordu — hem üye (`self.rows.push(r)`, 0xC0000374) hem yerel (`rows.push(r)`, 0xC0000005) yol | `dynArrayElemLLVMType`/`deriveNestedDynArrayInner` merkezi yardımcıları (`IRGen.cpp`), 6 kayıt sitesi (`IRGenDecl.cpp`, `IRGenCall.cpp` `resolveMemberDynArray`), for-in loop-var iç kaydı + `movedVars` borrow işareti (`IRGenStmt.cpp`); 9 RuntimeExec pinning testi |
| `[[T]]` kaynak-mutasyonu UAF: `rows.push(a)` sonrası `a.push(9)` — realloc iç buffer'ı taşırsa `rows[0]` dangling (izlemede) | Push/index-atama DUP YAPMAZ (tasarım gereği "değer kopyası") — pushlanan struct DEĞERİ kaynakla AYNI `data ptr`'ı paylaşır (shallow-alias); kaynak daha sonra büyüyüp realloc olursa yalnız kaynağın kendi struct alanı güncellenir, dizi slotundaki KOPYA eski (serbest bırakılmış) buffer'a işaret etmeye devam eder | Probe-doğrulanmış (görev-2 review bulgusu); use-after-move diagnostiği yok — kaynağı push/atama sonrası salt-okunur/tüketilmiş saymak gerekir |
| Aynı `[[T]]` slotunun çift okunması çift-serbest-bırakma üretir: `let p = rows[0]; let q = rows[0]` (izlemede) | Her iki binding de aynı `data ptr`'ı SAHİP olarak kaydediyor (`daIt->second`'dan gelen struct değeri dup'lanmıyor) — ikisi de kendi scope-exit'inde aynı bloğu `free()`'liyor | Tasarım adayı: binding-as-borrow (if-let DynArray kalıbındaki "Borrowed... skip cleanup" desenine benzer bir `movedVars`/borrow işaretleme, ikinci+ okumalar için). Aynı ailenin struct-alan varyantı: `let g = Grid { rows: a }` sonrası hem `a[1]` hem `g.rows[1]` bind edilirse çapraz-alias çift-free (klon dış tamponu kopyalar, iç `data` ptr'ları kaynakla paylaşımlı) |
| Tek-seviye `[T]` argümanının bir struct-metoda geçişinde aşırı-geniş arg-move — çağıranın buffer'ı her çağrıda sessizce sızıyor (izlemede) | Görev 2'nin double-free düzeltmesi (`movedVars.insert`) yalnız TEK bir struct-metod dispatch noktasında uygulandı; genel "değere göre argüman geçişi = move" kuralı genelleştirilmedi — davranış DOĞRU (çökme yok) ama her çağrıda çağıranın kaynak değişkeni artık hiç serbest bırakılmıyor (bellek profili değişti, tek-seferlik sızıntıdan çağrı-başına sızıntıya) | `IRGenCallMethod.cpp` struct-instance metod çağrı-dispatch'i (~15 farklı dispatch döngüsünden yalnız 1'i düzeltildi: class metodları, generic impl, zincirli üye-çağrılar dokunulmadı) |
| Dolu `[[T]]` struct-literal ALAN init'i sessizce yanlış veri üretiyordu — `Grid { rows: [[1,2],[3,4]] }` sonrası `g.rows[1]` bozuk, `g.rows[0]` doğru (maskeliyor) (çözüldü 2026-07) | Kök neden: `cloneIfDynArrayField` klon eleman boyutunu `toLLVMType(element)` ile hesaplıyordu — iç içe dizide element dinamik Array olduğundan 8 baytlık `ptr` dönüyor, oysa okuyucular 24 baytlık inline `%DynArray` adımlıyor; klon len*8 bayt kopyalıyordu (satır 0 kısmen geçerli, satır 1+ tampon dışı). Fix: merkezi `dynArrayElemLLVMType()` (7. kayıt sitesi) | `IRGenStmt.cpp:346` `cloneIfDynArrayField`; 2 koşum testi (alan init + index okuma, iç içe for-in) |
| Çok-seviyeli üye dizi element-ataması (`o.inner.vals[0] = x`) hâlâ sessiz no-op (2026-07'de tek-seviye düzeltildi, izlemede) | Yeni MemberExpr dalı yalnız `ident.field[i]` çözüyor (resolveMemberDynArray Identifier obje istiyor); okuma tarafı temiz derleme hatası verdiğinden sessiz-yanlış-okuma riski yok | `IRGenCall.cpp` visitAssignExpr MemberExpr dalı — genişletme adayı |

| Variadic fonksiyon ÇALIŞTIRMASI heap corruption ile çöküyordu (çözüldü 2026-07) | Çağıran variadic argümanları STACK'e paketliyor; callee kaydında borrow işareti olmadığından scope temizliği stack pointer'ına free() çağırıyordu. Fix: [T]-param kuralıyla aynı movedVars borrow işareti (tek satır) | `IRGenDecl.cpp` variadic kayıt bloğu; 2 koşum testi (boş variadic dahil) |
| Bağımsız generik fonksiyonun `[T]` parametresi/`T` dönüşü sessizce yanlış (önceden var, izlemede) | `func head<T>(xs: [T]) -> T` — `println(head(v))` BOŞ satır basıyor; anotasyonlu binding "expected 'i32', found 'T'" Sema hatası; impl-içi generikler (Stream) sağlam | TypeChecker dönüş-tipi propagasyonu — generic-mono-pitfalls ailesi |
| Dizi eleman store'ları dönüşüm yapmıyordu ve heterojen literal Sema'dan geçiyordu (çözüldü 2026-07) | İki katman: IRGen'de `coerceToElemType` 10 eleman-store sitesinde (VarDecl literali dinamik-anotasyonlu ×1 + statik-anotasyonsuz ×2, bağımsız literal ×2, push yerel+üye, eleman ataması dinamik+sabit+üye) — `[f64]=[1,2]` artık 2.0 okuyor, `[u8]` yuvasına 4 baytlık store taşması ve `[i64]` yuvasının ilklenmemiş üst yarısı kapandı. Sema'da eleman uyum kuralı: değer-koruyan dönüşüm her ifade için sessiz, kayıplı yalnız literalde + aralık kontrolü, bool sayısal değil; anotasyonlu literal anotasyona göre denetlenip onun tipini alıyor | `IRGen.cpp` `coerceToElemType`, `TypeChecker.cpp` `checkArrayElement`/`visitArrayLiteralExpr`/`visitVarDecl`; 15 koşum + 17 Sema testi |
| Generik struct alanının `[T]` literal init'i unification'ın PROMOTE ettiği adayı değil, ilk elemanın kendi tipini okuyordu — `Box<T> { var v: [T] }` + `Box { v: [1, 2.5] }` (Sema `[f64]`'e yükseltiyor) sessizce `T=i32` monomorfize ediyordu: 4 baytlık stride, 8 baytlık tampon üzerinde — `b.v[0]`/`b.v[1]` `0.000000` (çözüldü 2026-07, bu branch'in kendi regresyonu) | `inferArrayFieldElemType` yalnız `elems[0]->getResolvedType()` okuyordu; literalin KENDİ `resolvedType`'ı (unification'ın yazdığı) hiç kontrol edilmiyordu | `IRGenMono.cpp:429` `inferArrayFieldElemType`; 2 koşum testi (promoted + homojen f64) |
| `visitArrayLiteralExpr` unification döngüsünde aday-yükseltme testi `checkArrayElement` üzerinden yapılıyordu — ikinci argüman literal olduğunda kayıplı-literal izni sızıyor, yükseltme tip-düzeyinde değil değer-düzeyinde oluyordu; eski adaya kabul edilmiş elemanlar yeni adaya karşı YENİDEN denetlenmediğinden geçişlilik bozuluyor ve normalde reddedilecek bir DEĞİŞKEN eleman sızabiliyordu (çözüldü 2026-07) | Fix: yükseltme testi artık salt tip-düzeyinde `isValuePreserving(candidate, elemType)` — spec'in "sayısal kafes geçişli" varsayımını gerçekten sağlıyor (seçenek b; a — yeniden-denetleme — de geçerliydi ama daha büyük değişiklikti) | `TypeChecker.cpp` `visitArrayLiteralExpr`; 1 SemaTest (`let m: u32=5; let n=2000000000; let a=[7,n,m]` artık `err_array_element_type_mismatch`) |
| `checkArrayElement`'in kayıplı-literal izni tamsayı-literal→tamsayı-hedef ve ondalık-literal→ondalık-hedef ile sınırlıydı — tamsayı-literal→ONDALIK-hedef unutulmuştu, `let a: [f32] = [1, 2]` haksız yere reddediliyordu (spec'in kuralı 3'e göre `I32→F32` tam bu yoldan geçmeliydi, değer-koruyan tablosundan bilerek dışlandığı için) (çözüldü 2026-07) | Fix: tamsayı literal + ondalık hedef → aralık kontrolsüz kabul (spec'in açık kuralı); `[i32]=[1.5]` (ters yön) hâlâ reddediliyor | `TypeChecker.cpp` `checkArrayElement`; 2 SemaTest |
| Sema, dizi eleman konumundaki TANIMSIZ bir tipi reddetmiyor: `var arr: [I32] = [1,2,3]` (`I32` repoda hiçbir yerde tanımlı değil) derleniyor (izlemede) | `isUnjudgeableTarget` bilinmeyen `Named` hedefleri sessizce geçiriyor (generic kod için gereken davranış, ama bir yazım hatasını da aynı yoldan geçiriyor); bu branch başarısızlık modunu "derlenip çalışıyordu"dan `internal: cannot convert array element value to element type`'a çevirdi — kullanıcı yazım hatası için kötü bir mesaj. Üç `SelfHostTest` slice testi tam bu yazım hatasını taşıyordu (`[I32]`) ve bu branch'te `[i32]`'ye düzeltildi | `TypeChecker.cpp` `isUnjudgeableTarget`/`checkArrayElement`; `TypeChecker.cpp` genel isim-çözümleme — bilinmeyen `Named` tip için ayrı bir "tip bulunamadı" denetimi gerekir |
| `typesCompatible` diziler ve optional'lar için SIĞ'dı (çözüldü 2026-07) | Derin karşılaştırma yalnız `Named` ve `Tuple` için vardı; diğer bileşik tipler üst düzey kind eşleşince `return true`'ya düşüyordu. `let a: [string] = mk()` (mk → `[i32]`) derleniyor ve `a[0]` SEGFAULT ediyordu; `let m: [[string]] = [[1,2]]` ve `let a: string? = mk()` (mk → `i32?`) sessizce geçiyordu. Fix: Array ve Optional için özyinelemeli karşılaştırma. Dizi BOYUTLARI bilinçli karşılaştırılmıyor (sabit/dinamik eskisi gibi değiştirilebilir) | `TypeChecker.cpp` `typesCompatible`; 6 SemaTest. Tam süit yanlış-pozitif üretmedi (2557/2557) |
| Fonksiyon argümanları ve atamalar hiç tip denetimi görmüyordu (çözüldü 2026-07) | Sema: `checkAssignable` (eski `checkArrayElement`) argüman ve atama yollarına bağlandı — denetim yalnız bildirimi çözülebilen doğrudan çağrılarda çalışır, builtin/closure/dinamik dispatch sessiz kalır. IRGen: dönüşüm site sayımı yerine BOĞAZ NOKTASINDA, callee'nin FunctionType'ına göre (`coerceCallArgs`) — böylece `takeI64(5)`/`takeF64(3)`/`takeU8(200)` gibi bugüne dek LLVM verifier hatası veren çağrılar çalışıyor. Atama yolunda da skaler dönüşüm eklendi. Kapsam eki, İKİ turda: hesaplanmış özellik setter'ı ve subscript setter'ı — gerçek yazma setter FONKSİYONUNUN içinde, parametresi `coerceCallArgs` ile düzeltilince tam çözüldü (tur 1). willSet/didSet gözlemcileri FARKLI: alan yazımı `visitAssignExpr`'in KENDİSİNDE, `CreateCall`'un hemen ardından `CreateStore(val, gep)` ile oluyor — tur 1 yalnız gözlemci ÇAĞRISININ argümanını düzeltti, alanın kendisini düzeltmedi; verifier çökmesi gitti ama alan sessizce bozuluyordu (`t.value = -7` sonrası `t.value` `4294967289` okunuyordu). Tur 2: `val` aynı `coerceToElemType` deseniyle STORE'dan önce de alan tipine dönüştürülüyor (yalnız gözlemli alan yolunda — düz/computed-olmayan üye alan ataması kasıtlı olarak kapsam dışı bırakıldı); ayrıca didSet'in `oldValue` yüklemesi gelen değerin (`val->getType()`) değil ALANIN gerçek tipiyle okunacak şekilde düzeltildi. Tur 3 (final inceleme fix dalgası, 2026-07-25): CRITICAL regresyon — `coerceCallArgs`'ın `argTypes` parametresini HİÇBİR çağrı sitesi doldurmuyordu, bu yüzden `srcUnsigned` her zaman false'du ve işaretsiz bir kaynak (`u8` değişken → `i64`/`f64` parametre) SessizCE `CreateSExt` ile yanlış değere dönüştürülüyordu (`200` yerine `-56`) — bu branch'ten ÖNCE aynı çağrı gürültülü bir LLVM verifier hatasıydı, dolayısıyla bu sessiz-yanlış-değer bu branch'in kendi regresyonuydu. Fix: AST'nin elde olduğu 22 çağrı sitesinin TAMAMINDA (`IRGenCall.cpp` + `IRGenCallMethod.cpp`) değer vektörüyle eş-zamanlı bir `argTypes` vektörü dolduruldu; yalnız sentetik/self/env argümanları (AST karşılığı olmayanlar) `nullptr` bırakıldı. Ayrıca iki Sema yanlış-pozitifi: (1) `containsUnjudgeableType` `Array`/`Optional`'a iniyordu ama `Tuple`'a inmiyordu — `pairFirst<T>(p: (T,T))` çağrısı `(T,T)` parametre tipini kullanıcıya sızdırıyordu; Tuple elemanlarına özyinelemeli iniş eklendi. (2) `resolveAlias` yalnız TEK seviye çözüyordu — `type B = u8; type A = B` (iki seviyeli zincir) `take(7)`'yi reddediyordu; `kMaxTypeCompareDepth` ile aynı desende sınırlı (32 iterasyon) sabit-nokta döngüsüne çevrildi | `TypeCheckerCall.cpp` `checkCallArgTypes`, `TypeChecker.cpp` `visitAssignExpr`/`containsUnjudgeableType`/`resolveAlias`, `IRGen.cpp` `coerceCallArgs`, `IRGenCall.cpp` `visitAssignExpr` (setter/subscript-setter `CreateCall` siteleri tur 1; gözlemli alan `CreateStore` + `oldValue` yüklemesi tur 2; `argTypes` doldurma + Tuple/alias-zinciri fix'i tur 3), `IRGenCallMethod.cpp` (tur 3 — 13 çağrı sitesi). 13+6 Sema + 8+4 koşum testi (final inceleme fix dalgası: 6 Sema + 4 koşum). Kapsam dışı kalanlar: native builtin argümanları, variadic paketleme, düz (computed/gözlemsiz) üye alan ataması — `b.v = -7` bir `i64` struct/class alanında hâlâ `4294967289` veriyor; **struct/impl metod çağrıları, sınıf metodları ve init'leri** — spec'in kapsamına dahildi ama `checkCallArgTypes` yalnız `IdentifierExpr` callee'yi yargılıyor, metod/init argümanları hiç denetlenmiyor (bkz. aşağıdaki ayrı izleme satırı) — hepsi izlemede |
| Struct/impl ve protokol metod çağrılarının argümanları denetlenmiyordu (çözüldü 2026-07) | `checkCallArgTypes` yalnız `IdentifierExpr` callee'li serbest fonksiyonları yargılıyordu; `b.take(mkStr())` ([string] → [i32] parametre) sessizce derlenip çalışıyordu. Fix: mevcut `"Type::method"` anahtarlı `typeMethodReturnTypes_` deseninin yanına `typeMethodDecls_` (üç kayıt sitesi: yerel impl, import edilen impl, protokol); MemberExpr callee'de alıcının Named tipi çözülüyor. Anahtar bulunamazsa SESSİZ — builtin alıcılar ve tip adı çözülemeyen dispatch dokunulmuyor | `TypeCheckerCall.cpp` `checkCallArgTypes`, `TypeChecker.h` `typeMethodDecls_`; 5 SemaTest, tam süit 2603/2603 |
| Metod argüman denetiminin kalan boşlukları (kısmen çözüldü 2026-07) | ÇÖZÜLDÜ: sınıf metodları ve class `init`'leri artık `typeMethodDecls_`'e kaydediliyor (`registerTypeMethodDecls`, yerel + import edilen sınıf); `classParent_` yürüyüşü artık gerçekten çalışıyor (üç seviyeli kalıtımda kalıtılan metod bulunuyor, çocuktaki override önce bulunup kazanıyor); init'ler arity'ye göre seçilip denetleniyor. KALAN: (1) `dyn Protocol` alıcısı sessiz (alıcı kind'ı Named değil; dyn'siz bare protokol parametresi denetleniyor). (2) Generik impl'de SOMUT parametreli metod bile sessiz (`impl Holder<T>` içindeki `s: string` parametresine `5` geçiyor). (3) METOD denetimi bildirim SIRASINA bağlı: `main` impl'den önce yazılırsa çağrı denetlenmiyor (ön-geçiş yok; `typeMethodReturnTypes_` ile aynı önceden var olan desen) — class `init` denetimi sıra-BAĞIMSIZ çalışıyor. (4) Sınıfın/impl'in KENDİ gövdesindeki `self.kardeşMetod(x)` çağrıları sessiz: `self` tipsiz bir Parameter sembolü olduğundan alıcının resolvedType'ı null kalıyor. (5) Tip adı üzerinden çağrı (`Cls.staticMethod(x)`) sessiz: tip adı identifier'ının resolvedType'ı yok | `TypeChecker.cpp` `registerTypeMethodDecls`, `TypeCheckerCall.cpp` `checkCallArgTypes`; 5 SemaTest, tam süit 2608/2608 |
| `examples/dyn_protocol_demo.liva` derlenmiyordu ve ExamplesTest kapısı 77 örnekten yalnız 11'ini kapsıyordu (çözüldü 2026-07) | İki kusur tek kök: dizi literalinin eleman-birleştirme döngüsü değişkenin ANOTASYONUNDAN habersizdi. (a) `[dyn Shape] = [Circle{..}, Square{..}]` reddediliyordu — birleştirme iki conformer'ı birbirine karşı yargılıyordu; artık anotasyon varsa denetimin sahibi anotasyon yolu, birleştirme susuyor (bu aynı zamanda "aynı elemana çift diagnostik" kusurunu da kapattı). (b) Sema geçince IRGen'de kalıyordu: `[dyn P]` kutulama dalı yalnız IdentifierExpr elemanlarını işliyordu, struct LİTERALİ ham değer olarak trait-object yuvasına yazılıyordu; literal artık alloca'ya materyalize edilip kutulanıyor. Kapı 61 örneğe genişletildi (açık liste) | `TypeChecker.cpp` `visitArrayLiteralExpr`/`visitVarDecl`, `IRGenDecl.cpp` dyn-proto kutulama dalı, `ExamplesTest.cpp` `AllKnownGoodExamplesCompile`; 3 SemaTest |
| 16 örnek hâlâ derlenmiyor ve bu yüzden ExamplesTest kapısının dışında (önceden var, 2026-07'de ölçüldü, izlemede) | Sema'da kalanlar (10): `benchmark_demo`/`concurrency_demo` olmayan modül (`std::bench`, `std::channel`); `control_flow`/`ffi_demo` `redefinition of 'abs'` (builtin çakışması); `ownership`/`ownership_demo` borrow checker; `comptime_demo` comptime evaluability; `macro_demo` makro genişletme parse hatası; `pattern_matching` parse hatası; `type_alias` `Int` tipinde `+` (Add protokolü yok). IRGen/link'te kalanlar (6): `async_example` çağrı-sonucu member zinciri; `closures` codegen'de çözülemeyen fonksiyon; `cross_compile_demo` tanımsız değişken; `math` main yok (kütüphane örneği); `module_main` modül yolu CWD'ye bağlı; `structs` struct'ı değere göre geçme (ptr üretiliyor, imza `%Point` istiyor) | Her biri ayrı kök neden; kapıya eklenmeden önce tek tek düzeltilmeli |
| `dyn P` (protokol biçimi) uygunluk denetimi yoktu — uymayan tip çalışma zamanında segfault ediyordu (çözüldü 2026-07) | Kutulama eksik metodun vtable yuvasını null ile dolduruyor, `[dyn Shape] = [.., Blob{}]` sıfır diagnostikle derlenip ilk çağrıda çöküyordu. Denetim ilk denemede geri çekilmişti çünkü `protocolConformances_` yalnız mevcut çeviri biriminden besleniyordu (import yolu sadece `Drop` taşıyordu) ve import edilen conformer non-conformer gibi görünüp ÇALIŞAN kodu reddediyordu. Fix: import yolu artık HER `impl X : P` uygunluğunu taşıyor; diğer okuyucular (Iterator/AsyncIterator for-in, OwnershipChecker Drop kümesi) de import edilen conformer'ları görüyor. Denetim protokol biçiminde uygunluk listesine, sınıf biçiminde (`[dyn Control]`) atalar zincirine bakıyor; X ikisi de değilse sessiz | `TypeChecker.cpp` import propagasyonu + `visitVarDecl` dyn dalı; 3 SemaTest, tam süit 2619/2619 |
| `dyn` ATAMA ve STRUCT-ALAN yolları denetimsizdi — sessiz tip karmaşası (çözüldü 2026-07) | `var s: dyn Shape = Circle{..}; s = Blob{..}; s.area()` sıfır diagnostikle derlenip Circle'ın vtable'ı + Blob'un verisiyle YANLIŞ değer basıyordu (2.000000); `struct Holder { var s: dyn Shape }` + `Holder { s: Blob{..} }` de denetimsizdi. Atamada `checkAssignable` DynProtocol'ü yargılanamaz sayıp Ok döndürdüğünden ayrı çağrı gerekti; struct-literal alanlarında ise alan tiplerine karşı HİÇ denetim yoktu (alan-tipi haritası yalnız generik çıkarım için kuruluyordu). ÜYE-hedefli atama (`h.s = Blob{..}`) da kapatıldı (2026-07): `visitMemberExpr` hedefi zaten alanın BİLDİRİLEN tipine çözüyor, `visitAssignExpr` artık MemberExpr hedeflerinde de `checkDynConformance` çağırıyor. KALAN: `push` ve ternary denetimsiz ama gürültülü düşüyor (eleman-dönüşüm hatası / PHI verifier) | `TypeChecker.cpp` `visitAssignExpr` + `visitStructLiteralExpr`; 4+2 SemaTest, tam süit 2638/2638 |
| IRGen'de `dyn` RETURN ve struct-LİTERALİ argüman kutulaması yok; ayrıca üye alıcıda dispatch yok (çözüldü 2026-07) | Hepsi tek bir kusur ailesiydi: her `dyn P` yuvası yalnız TEK bir ifade biçimini (IdentifierExpr) tanıyordu. (1) ÜYE ALICI: `h.s.area()` hiç çağrı üretmiyordu — dispatch `vars_.varProtocolTypes`'a ad ile bakıyordu; artık alıcı bir "trait object adresi + protokol adı" çiftine genelleştirildi ve `resolveMemberDynProtocol` (resolveMemberDynArray'in ikizi) `dyn P` ALANININ GEP'ini veriyor. Alanın somut tipi statik bilinmediğinden devirtualizasyon bilinçli olarak atlanıyor — iki farklı conformer'lı holder testi bunu pinliyor. (2) RETURN: `boxIfDynProtocolField`'in gövdesi `boxIntoDynProtocol(protokol, val, expr)` yardımcısına çıkarıldı; `visitReturnStmt` dönüş tipi `dyn P` ise (`currentFuncDynProtocol_`, üç fonksiyon-giriş sitesinde kaydet/sıfırla/geri yükle) değeri kutuluyor. DİKKAT: return'de payload HEAP'te (malloc) — trait object payload'ını yalnız ÖDÜNÇ alır, `return` ise çerçevenin ölmesi nedeniyle tek istisna; sahiplik bilgisi olmadığından bilinçli olarak hiç free edilmiyor (sınıf örnekleriyle aynı takas). (3) STRUCT-LİTERALİ ARGÜMANI: çağıran-taraf kutulama bloğu "bağlı değişken değilse" dalında aynı yardımcıya düşüyor (payload stack'te — callee çağıranı yaşatamaz). (4) `let s = get()` bağlaması: çağrı-sonucu trait object'i hiçbir sınıflandırma almadığından `s.area()` yine çağrı üretmiyordu; yeni VarDecl dalı yalnız İLKLEYİCİNİN çözülmüş tipine bakıyor (çift-ziyaret riski yok) | `IRGenCall.cpp` `resolveMemberDynProtocol` + argüman kutulaması, `IRGenCallMethod.cpp` dispatch genelleştirmesi, `IRGenStmt.cpp` `boxIntoDynProtocol` + return, `IRGenDecl.cpp` `currentFuncDynProtocol_` + VarDecl dalı; 4 RuntimeExecTest, tam süit 2638/2638 |
| Derleyici SEGFAULT: generik çağrı sonucunun binding'i — ve aynı DİLİMLEME kusurunun beş yolu daha (çözüldü 2026-07) | Şüphe doğrulandı: çıkarılan bir sonuç tipi `makePrimitiveType(kind)` ile kuruluyordu — bu yalnız KIND etiketini taşıyan çıplak bir `TypeRepr` üretir, payload'ı düşürür. `T` bir struct'a bağlandığında sonuç `Named` kind'lı ama `NamedTypeRepr` OLMAYAN bir nesne; ilk downcast eden tüketici (`TypeChecker.cpp:1159` File-değişkeni izleme, `getName()`) çöp `std::string` okuyup SIGSEGV / `bad allocation` veriyordu. `[i32]` bağlandığında ise çökme yok ama tip eleman-sız kalıyor ve `<array>` diye yazdırılıyordu (`let c: [i32] = identity(a)` haksız yere reddediliyordu). UB olduğu için çökme allocator durumuna göre KARARSIZ — aynı repro bazen sessizce geçiyordu. Fix: altı sitede `cloneTypeRepr` (kod tabanında zaten belgeli idiom — `visitGroupExpr` ve `NilCoalesce` dalları aynı gerekçeyi taşıyor). Probe ile doğrulanan çöken yollar: generik dönüş, closure/fonksiyon-tipli sembol dönüşü, `reduce` akümülatörü, `expr as Struct`, aşırı yüklenmiş unary `-`; ek olarak evidence'sız ama aynı tek-satırlık kusur olan `abs/min/max` ve ikili aritmetik dalları da kapatıldı | `TypeCheckerCall.cpp` (generik dönüş, fonksiyon-tipli sembol, `reduce`, `abs/min/max`), `TypeChecker.cpp` (`visitBinaryExpr` aritmetik dalı, `visitUnaryExpr`, `visitCastExpr`); 7 SemaTest + 1 RuntimeExecTest, tam süit 2646/2646 |
| `dyn X` uygunluk denetimi yalnız dizi-literali yolundaydı (çözüldü 2026-07) | Skaler init ve fonksiyon argümanı yolları denetimsizdi ve ilk metod çağrısında segfault ediyordu; `return` yolunda ise (IRGen'de kutulama hiç olmadığından) gürültülü verifier hatası vardı. Mantık `checkDynConformance(target, value)` yardımcısına çıkarılıp dört yerden çağrılıyor. Return çağrısı uyumsuzluk dalının DIŞINDA: `typesCompatible` DynProtocol hedefte erken `true` döner (trait-object dönüşlerinin çalışma biçimi), dolayısıyla o dal hiç işlemez; argümanda da `checkAssignable` DynProtocol'ü yargılanamaz sayıp Ok döndürüyor. Tip PARAMETRELERİ atlanıyor — sınırlı `T` somut tiplerle aynı `Named` kind'ında ve uygunluğu sınırı garanti ediyor (tam süit bu yanlış pozitifi yakaladı) | `TypeChecker.cpp` `checkDynConformance` + `visitVarDecl`/`visitReturnStmt`, `TypeCheckerCall.cpp` `checkCallArgTypes`; 7 SemaTest, tam süit 2626/2626 |
| Anotasyonlu init'in İÇİNDEKİ dizi literalleri kısmen denetimsiz (2026-07, izlemede) | `arrayLiteralHasAnnotation_` bayrağı literal ziyaretinde temizleniyor, bu yüzden struct-alan (`[Box { v: [1,"x"] }]`) ve iç içe (`[[1,"x"]]`) biçimler temiz Sema hatası veriyor. Ama init bir ÇAĞRI ise (`let a: [i32] = f([1,"x"])`) bayrak tüm çağrı ziyareti boyunca set kalıyor ve anotasyon-denetimi init ArrayLiteral olmadığı için hiç çalışmıyor → `internal:` IRGen hatası. Ayrıca `[dyn Shape] = [Circle{..}, 42]` gibi Named-olmayan eleman conformance dalında atlanıyor → yine IRGen internal. İkisi de derleme-zamanı hatası, sessiz-yanlış değil | `TypeChecker.cpp` `visitVarDecl` bayrak set noktası / `visitArrayLiteralExpr` |
| `return` değeri bildirilen dönüş tipine hiç dönüştürülmüyordu (çözüldü 2026-07) | İki katman. (1) IRGen: `visitReturnStmt` `CreateRet`'ten önce hiçbir dönüşüm yapmıyordu — `func mk() -> i64 { return 5 }` `ret i32 5` üretip LLVM verifier'da düşüyordu. Fix, argüman yolundaki `coerceCallArgs` deseninin ikizi: BOĞAZ NOKTASINDA, fonksiyonun bildirilen LLVM dönüş tipine göre `coerceToElemType` (async'te `asyncDeclaredRetType_`). Tek site tüm dönüş yollarını kapsıyor (serbest fonksiyon, impl metodu, sınıf metodu); `coerceToElemType` çeviremediği her şey için (struct, pointer, yukarıda kurulan Optional/trait-object payload'ı) null döndüğünden o değerler aynen kalıyor. (2) Sema: elle yazılmış "int literali → daha geniş int" kuralı hem DAR hem GENİŞTİ — `return -7` (literal değil, `UnaryExpr`) ve `return a` (i32 değişken) `-> i64`'ten reddediliyordu, oysa aynı `takeI64(-7)`/`takeI64(a)` argüman geçişi kabul ediliyor; öte yandan aralık denetimi yoktu, `-> u8 { return 300 }` geçiyordu. IRGen artık dönüştürdüğü için bu sessizce 44'e kırpılırdı, dolayısıyla kural `checkAssignable`'a bağlandı (dizi elemanı/argüman/atama ile aynı yargı) + yeni `err_return_literal_range`. `-1` → `u32` hâlâ reddediliyor, argüman yolundaki gibi genel uyuşmazlık tanısıyla | `IRGenStmt.cpp` `visitReturnStmt`, `TypeChecker.cpp` `visitReturnStmt`, `DiagnosticKinds.def`; 5 SemaTest + 4 RuntimeExecTest, tam süit 2655/2655. `RuntimeExecTest.cpp`'deki `return X as i64` geçici çözümü artık gerekli değil (yorumu güncellendi, testler olduğu gibi bırakıldı) |
| `println` i32/i64 dışındaki tamsayı DEĞERLERİNİ yanlış basıyor (çözüldü 2026-07) | İki katman, tek site. (1) PROMOTION: printf variadic olduğundan C ABI `int`'ten dar her tamsayıyı çağrı yerinde `int`'e yükseltir; println lowering'i yalnız i32/i64/float/pointer'ı tanıyıp i8/i16'yı ham geçiriyordu, printf 1-2 baytlık yuvadan 4 bayt okuyordu. UB olduğu için görünüm KARARSIZ: yuva çöp tutuyorsa çöp (`200 as u8` → -508793656), sıfırlıysa düşük bitler işaret düşmüş hâlde (`-5 as i8` → 251, `-300 as i16` → 65236). Aynı döngünün i1 dalı bu tuzağı zaten biliyordu ("varargs require at least i32") — kural bool'a uygulanmış, i8/i16'ya uygulanmamıştı. Roadmap'in "u8 PARAMETRESİ doğru basılıyor" notu YANLIŞTI: probe'da parametre de bozuk çıktı, ayrı bir yol değil aynı UB'nin şanslı hâliymiş (çağrı ABI'si registerı temiz bırakmıştı). (2) FORMAT: format LLVM tipinden seçiliyordu, u32'yi i32'den ayıramaz — işaretli aralığın üstündeki değerler negatif basılıyordu (u32 4000000000 → -294967296; bitler doğru, format yanlış). İkisi de argümanın BİLDİRİLEN TypeRepr'ından çözüldü. DİKKAT — genişletmenin fallback'i bilinçli olarak ZExt: bir `[u8]` ELEMANI (`blob[4]` → 255) bu noktada eleman tipi TAŞIMIYOR (Sema `[u8]` eleman tiplerini bilerek çözmüyor — gzip byte işaretliliğini bozmuştu — ve `DynArrayInfo` yalnız LLVM tipini tutuyor), tahminle sign-extend etmek 255'i -1 yapardı ve `SqliteBlobRoundTrip`'i düşürüyordu. Yani SExt yalnız bildirilen tip gerçekten I8/I16 ise; bilinmiyorsa ZExt = önceki düşük-bit okuması, yeni yanlış eklemez. KALAN: `[i8]`/`[i16]` ELEMANLARI hâlâ sıfır-dolgulu basılıyor (eleman tipi çözülmediği sürece kaçınılmaz; eskiden de öyleydi) | `IRGenCallCore.cpp` println lowering'i (`isUnsignedTypeRepr` + I8/I16 kontrolü); 9 RuntimeExecTest, tam süit 2664/2664 |
| Tamsayı literali i32 aralığını aşınca SESSİZCE sarıyor — `as u32`/`as u64` de yakalamıyor (yeni bulundu, 2026-07, izlemede) | `println(4000000000 as u32)` → değer aslında doğru basılıyor ama tesadüfen: literal önce i32'de -294967296'ya sarıyor, bit deseni u32 4000000000 ile aynı olduğundan `%u` doğru okuyor. Sarma `as u64`'te maskesiz görünüyor: `4000000000 as u64` sign-extend edilip -294967296 oluyor, `big * big` 87005705709551616 basıyor (= (-294967296)²). Yani `as` hedefi literalin aralığını hiç genişletmiyor; 2^31 üstü hiçbir u32/u64 sabiti doğrudan yazılamıyor (println testleri bu yüzden değerleri aritmetikle kuruyor). Sessiz yanlış değer sınıfı | Literal tiplemesi — `checkAssignable`'ın literal aralık kuralı `as` hedefine uygulanmıyor; lexer/Sema literali koşulsuz i32 sayıyor |
| `ref` parametreli bir çağrı LLVM verifier'da düşüyor (çözüldü 2026-07) | Bu kaydın ESKİ kök-neden teşhisi ("argüman geçiş kodegen'i adres-of üretmiyor") YANLIŞTI: adres-alma kodegen'i var ve çalışıyor — `bump(ref mut k)` her iki parametre sözdiziminde de doğru sonuç veriyor (probe: 11). Gerçek boşluk Sema'daydı: Liva çağrı yerinde AÇIK ödünç istiyor (`increment(ref mut n)`, dil referansında böyle), ama çıplak bir değer geçmek hiç reddedilmiyordu ve ne olduğu çağrı biçimine göre değişiyordu — serbest fonksiyon verifier'da düşüyor, aynı hata bir METOTTA sessizce derlenip mutasyonu düşürüyordu (`s.bump(k)` → 10 basıyor, 11 beklenirken). Literal argüman (`bump(7)`, adres alınamaz) ve bir `ref` parametresini başkasına çıplak İLETME (`inner(y)`) de aynı yoldan geçiyordu. Fix: `checkCallArgTypes`'ın `ref` parametreleri için koşulsuz atlaması kalktı — TİP karşılaştırması hâlâ atlanıyor (temsil boşluğu, bkz. aşağıdaki kayıt) ama biçimden yargılanabilen kural uygulanıyor: `ref`/`ref mut` parametrenin argümanı bir `RefExpr` olmalı. Her iki sözdizimi (`ref mut n: T` ve `n: ref mut T`) `parseType`'tan ÖNCE tüketilen token'lar sayesinde aynı temsile (isRef/isMutRef + referent type) çözüldüğünden tek denetim ikisini de kapatıyor; metot yolu `typeMethodDecls_` üzerinden aynı döngüye girdiği için sessiz-no-op da kapandı | `TypeCheckerCall.cpp` `checkCallArgTypes`, `DiagnosticKinds.def` `err_ref_arg_required`; 6 SemaTest, tam süit 2670/2670. Tam süit hiç yanlış-pozitif üretmedi — meşru bir çıplak-argüman deseni yokmuş |
| `ref mut` parametreye `ref` (değişmez) argüman geçmek kabul ediliyor ve mutasyon YİNE oluyordu (çözüldü 2026-07) | `func bump(x: ref mut i32)` + `bump(ref k)` derleniyor ve `k` 11 oluyordu. Yalnız yazım hatası değil SAĞLAMLIK deliğiydi: OwnershipChecker argümanı PAYLAŞIMLI ödünç olarak kaydediyor, dolayısıyla callee aynı değişkene yazarken yanına ikinci bir paylaşımlı ödünç oturabiliyordu (`let r = ref k` + `bump(ref k)` sıfır tanıyla derleniyor). Fix: `err_ref_arg_required` dalına `isMutRef && !RefExpr::isMutable()` kontrolü. TERS yön (`ref mut` argüman → paylaşımlı `ref` parametre) meşru ve kabul edilmeye devam ediyor | `TypeCheckerCall.cpp` `checkCallArgTypes`, `DiagnosticKinds.def` `err_ref_arg_not_mutable`; 3 SemaTest, tam süit 2678/2678 (mevcut kodda tek bir uyuşmazlık yokmuş) |
| `println` bir `ref` BAĞLAMASINI string sanıyor — sessiz yanlış çıktı (yeni bulundu, 2026-07, izlemede) | `var k: i32 = 10; let r = ref k; println(r)` BOŞ satır basıyor. Referans LLVM'de `ptr` olduğundan println'in format seçimi `isPointerTy` → `%s` dalına düşüyor ve işaret edilen i32'yi C string'i sanıyor; değer 10 olduğu için ASCII 10 (satırbaşı) basılıyor — teşhisi birebir doğrulayan bir tesadüf. Referans bağlaması yazdırılırken önce DEREFERANS edilmeli (ya da temiz bir tanı verilmeli). `[u8]`/dar-tamsayı promotion işiyle aynı sitede ama ondan bağımsız: orası tamsayı genişletmesi, burası pointer'ın string sanılması | `IRGenCallCore.cpp` println lowering'i — pointer dalı referans-bağlamalı değişkenleri string'den ayırt etmiyor; `vars_.varRefTypes` bu ayrımı taşıyor |
| `ref mut` argüman ödüncü hiç serbest bırakılmıyor — bir değişken kapsam başına yalnız BİR kez değişebilir ödünç alınabiliyordu (çözüldü 2026-07) | Kök neden tek satırdı: `releaseBorrows`'a giden TEK yol `dropScopeVariables()`'dı, yani ödünç ancak değişken zaten yok olurken düşüyordu. Sonuç: `take(ref mut k)` iki kez çağrılamıyor, sonrasında `peek(ref k)` de reddediliyordu — `ref mut` parametreli yardımcılar pratikte tek kullanımlıktı. Fix: çağrı argümanı olarak alınan ödüncler çağrının sonunda bırakılıyor. İki incelik: (1) BAĞLAMA ödüncü (`let r = ref mut x`, dilde destekli ve OwnershipTest'te pinli) kapsam boyu yaşamalı, bu yüzden bırakma yalnız `visitCallExpr`'in RefExpr ARGÜMANLARINA uygulanıyor; (2) mevcut `releaseBorrows` değişkendeki TÜM ödüncleri sıfırlıyor — canlı bir binding ödüncü varken bu onu da silerdi, bu yüzden tek bir `addBorrow`'un tam karşılığı olan `releaseBorrow(name, isMutable)` eklendi (mutable bayrağı temizler / immutable sayacı azaltır). Ödünçler argümanların TAMAMI ziyaret edildikten sonra bırakılıyor ki aynı değişkeni iki argümanda ödünç almak hâlâ çakışsın. `addBorrow` izlenmeyen değişkende de `true` döndüğünden `visitRefExpr` yalnız gerçekten kaydedilen ödüncü `lastRefBorrow_` ile bildiriyor | `OwnershipChecker.cpp` `visitCallExpr`/`visitRefExpr`/`releaseBorrow`, `OwnershipChecker.h`; 4 OwnershipTest + 1 RuntimeExecTest, tam süit 2675/2675. KALAN: ödünç hâlâ ifade-düzeyinde değil — bir binding'in ödüncü son kullanımında değil kapsam sonunda düşüyor (muhafazakâr, sağlam) |
| Argüman tip denetimi `ref` parametreleri ve fonksiyon-tipli parametreleri sessizce ATLIYOR — iki ayrı temsil boşluğu yüzünden (önceden var, izlemede) | (1) `ParamDecl` bir `ref`/`ref mut` parametrenin REFERANS ALDIĞI tipi `type` alanında tutuyor, referans-lık bilgisini ayrıca `isRef`/`isMutRef` bayraklarıyla taşıyor — sarmalayan bir `ReferenceTypeRepr` YOK; `ref expr` argümanı ise `ReferenceTypeRepr`'e çözülüyor (`visitRefExpr`). `checkAssignable` bu iki kuralı da bilmiyor, kind her zaman uyuşmuyor, bu yüzden `checkCallArgTypes` bu parametrelerin TİP karşılaştırmasını atlıyor. (2026-07 güncellemesi: koşulsuz atlama kalktı — temsil boşluğu yüzünden tip karşılaştırması hâlâ yapılmıyor, ama biçimden yargılanabilen `err_ref_arg_required` kuralı artık uygulanıyor; bkz. yukarıdaki kayıt.) (2) Bir fonksiyon sembolünün `type` alanı fonksiyonun DÖNÜŞ tipi (fonksiyon tipi değil) — bir fonksiyon-tipli parametreye karşı çıplak bir fonksiyon-adı argümanı (`apply(5, double_it)`) karşılaştırıldığında da aynı sebeple kind hep uyuşmuyor, bu yüzden bu parametreler de atlanıyor (bkz. `TypeCheckerCall.cpp:184-196`). Her iki durumda da denetim sessiz kalıyor — reddetmek yerine, yanlış-pozitif riskinden kaçınmak için bilinçli tercih | `TypeCheckerCall.cpp` `checkCallArgTypes` — `ParamDecl`'ın `ref` temsili + fonksiyon sembolünün `type` alanının anlamı, iki ayrı temsil boşluğu |
| `T` argümanının `T?` parametreye geçişinde Sema kabul ediyor ama IRGen hiç sarmıyor — ATAMA yolunun aksine (izlemede) | `checkAssignable`'da "hedef Optional, kaynak değil" kuralı (`target->getKind() == Optional && valueType->getKind() != Optional` → inner tipe karşı yeniden dene) Sema'da bir bare `T` argümanını `T?` parametreye kabul ediyor (`SemaTest.ArgOptionalWrapStructAccepted` bunu pinliyor) ama argüman boğaz noktası (`coerceCallArgs`/`coerceToElemType`) struct-değer → `%Optional{i1,T}` sarma dönüşümünü hiç bilmiyor — LLVM verifier'da düşüyor. ATAMA yolu (`x: i32? = nil; x = 42`) ise doğru çalışıyor (`AssignOptionalWrapIntAccepted`/`AssignOptionalWrapStructAccepted` testleri), çünkü o yol skaler `coerceToElemType`'a hiç girmeden mevcut Optional-oluşturma kodegen'inden geçiyor — asimetri kasıtlı değil, argüman tarafında eksik bir kodegen adımı | Doğrulanmış repro: `struct Res { var id: i32 }` + `func take(r: Res?) -> i32 { return 1 }` + `take(a)` (a: `Res`) → `LLVM module verification failed: Call parameter type does not match function signature! %Res %a1 / %Optional = type { i1, %Res } call i32 @take(%Res %a1)`. `IRGen.cpp` `coerceCallArgs`/`coerceToElemType` — Optional-sarma dönüşümü eksik |
| Generik tuple parametresine somut tuple geçmek IRGen'de verifier hatası veriyor (önceden var, 2026-07 final fix dalgasında erişilebilir oldu, izlemede) | `func pairFirst<T>(p: (T, T))` + `pairFirst((1, 2))` — Sema artık doğru şekilde geçiriyor (tuple içi generik susturması eklendi), ama monomorfizasyon tuple'ı `{ptr, ptr}` olarak lower edip çağrı yerindeki `{i32, i32}` ile çakışıyor. Gürültülü hata, sessiz yanlış veri değil | `IRGenMono.cpp` tuple parametre lowering'i — branch diff'i `src/IR`'da tuple'a hiç dokunmuyor |
| Anotasyonlu dizi yeniden-bağlaması runtime'da çöküyor (önceden var, 2026-07 final incelemesinde bulundu, izlemede) | `let a: [i32] = [1, 2]; let b: [i32] = a; println(b.length)` — derleniyor, çıktı vermeden exit 127 (muhtemelen double-free). Generic impl içinde `let xs: [T] = self.items` de aynı şekilde; yeniden-bağlamasız varyant doğru çalışıyor. Fixed→dynamic bağlama (`let d: [i32] = f`) de aynı | Değer-kopyası/move ailesi — `IRGenDecl.cpp` VarDecl kopya yolu |
| Kalan bileşik tipler `typesCompatible`'da hâlâ SIĞ: `Reference`, `Result`, `Function`, `Generic`; ayrıca `Named` TİP ARGÜMANLARI (`Stack<i32>` vs `Stack<string>` yalnız isimle karşılaştırılıyor) (izlemede) | Aynı kusur sınıfı — üst düzey kind eşleşince iç tipler hiç karşılaştırılmıyor. Doğrulandı: `let f: (i32) -> string = \|x: i32\| -> i32 { return x }` temiz derleniyor | `TypeChecker.cpp` `typesCompatible` — Array/Optional dallarının yanına aynı desende eklenebilir |
| `bool`, `push` üzerinden bir tamsayı dizisine eleman olarak sızabiliyor: `[i32]` üzerinde `a.push(true)` `1` depoluyor — literal yolu (`[1, true]`) doğru reddederken `push` argümanı hiç tip-denetlenmiyor (izlemede) | Sema `push` çağrısının argümanını dizinin eleman tipine karşı hiç denetlemiyor; yalnız dizi LİTERALİ (unification + anotasyon-yönlendirmeli yol) denetleniyor | Sema call-argument denetimi `push` için eksik — `checkArrayElement`'in aynı kuralı `push` argümanına da uygulaması gerekir |
| Aynı eleman AYNI diagnostiği iki kez üretebiliyor — unification döngüsü bir kez, anotasyon-yönlendirmeli döngü bir kez: `let a: [i32] = [1, "x"]` (izlemede) | `visitVarDecl`'in anotasyon-yönlendirmeli kontrolü (§1.3), unification döngüsünün (§1.2) zaten raporladığı elemanı FARKINDA olmadan yeniden denetleyip aynı hatayı ikinci kez bildiriyor | `TypeChecker.cpp` `visitVarDecl` (satır ~923-955) — unification'ın zaten hata verdiği elemanları anotasyon döngüsünde atlaması gerekir |
| `coerceToElemType`'ta `FPToUI` yolu yok: bir ondalık değer `[u32]` dizisine `push` edilince `FPToSI` ile dönüştürülüyor (izlemede) | `push` argümanı Sema'da hiç tip-denetlenmediği için (bkz. yukarıki `bool`/`push` kaydı) bu yol ulaşılabilir; `coerceToElemType` işaretlilik bilgisini `bool srcUnsigned` parametresinden alıyor ama HEDEFİN işaretliliğini hiç kullanmıyor (yalnız kaynağınkini) | `IRGen.cpp` `coerceToElemType` — ondalık→tamsayı dalı yalnız `CreateFPToSI` üretiyor, hedef işaretsizse `CreateFPToUI` gerekir |
| Kullanıcı fonksiyonunun `-> [T]?` dönüşü LLVM verifier hatası (önceden var, izlemede) | `insertvalue %Optional {i1, ptr}` payload'ı %DynArray struct — Optional lowering'i DynArray-inner'ı ptr sanıyor; native builtin'ler ([u8]? dönenler) farklı yoldan çalışıyor | Optional-of-DynArray lowering; repo'da kapsam yok |

| Variadic İLETİMİ sessizce yanlış (yeni erişilebilir oldu, 2026-07 izlemede) | `outer(values: i32...) { inner(values) }` derleniyor ama paketleme döngüsü DynArray değişkenini TEK skaler eleman sanıyor — kesilmiş pointer değeri basılıyor (probe: 6 beklenirken 426768356, exit 0) | `IRGenCall.cpp:304` varargs paketleme — DynArray-tipli argümanı spread/geçir olarak ele almalı |
| Generik struct'ın `[T]` alanına literal init çöküyordu (çözüldü 2026-07) | `struct Box<T> { var v: [T] }` + `Box { v: [10,20,30] }` → SEGFAULT. Kök neden: `inferStructTypeArgs` yalnız DOĞRUDAN `Named T` alanlardan çıkarım yapıyordu, `[T]` alanı atlanınca typeArgs boş kalıp alan substitüsyonsuz monomorfize ediliyordu (`T` opak ptr, elemSize 8). Fix: DynArray değerinin LLVM tipi eleman-silinmiş olduğundan T başlatıcının AST'sinden çıkarılıyor (dizi literali → ilk elemanın resolvedType'ı; diğer ifadeler → resolvedType `[U]` ise U). Çıkarım mümkün değilse (boş literal) sessiz miscompile yerine `err_generic_struct_type_args_uninferred` — kaçış yolu `Box<i32> { ... }` mesajda | `IRGenMono.cpp` `inferStructTypeArgs`/`inferArrayFieldElemType`/`diagnoseGenericStructTypeArgs`; 4 koşum + 3 diagnostik testi. İç içe `[[T]]` ALANI hâlâ çıkarılamaz (element Array olduğundan) — artık temiz diagnostiğe düşüyor ama önerilen kaçış `Box<[i32]>` parser'da desteklenmediği için kullanıcı çıkışsız kalıyor (izlemede: struct-literal turbofish'inde dizi tip argümanı) |
| Impl-metodlarda variadic parametre desteklenmiyor — IRGen internal hatası (önceden var, izlemede) | Parse+Sema geçiyor, gövdede her kullanım "internal: undefined variable" — metod yolunda variadic kaydı da (IRGenDecl.cpp:2035 dışlıyor) çağrı-tarafı paketleme de (IRGenCallMethod) yok | Metod yolu kaydı + paketleme + temiz diagnostik gerekli |

### 2.4 Sağlam olduğu doğrulanan alanlar

Generator/yield codegen'i gerçekten tam (LLVM `coro.*` lowering, for-in, break-early destroy, runtime testli), protokol default metodları (Sema + codegen), operator overloading (aritmetik + karşılaştırma seti, `!=`/`>=`/`>` sentezi), `?` error propagation, closures (capture + heap env), sınıf sistemi (vtable, override, static, computed properties, `final`, `is`/`as?`).

### 2.5 Doğal sonraki dil özellikleri

1. Yapısal pattern matching (bkz. 2.1)
2. Gerçek borrow checker: atama/if-let'te move takibi + `'a` anotasyonlarını anlamlı kılacak outlives kontrolü (double-drop sınıfı hataları kökten çözer)
3. Drop bütünlüğü: Optional, koleksiyon elemanları ve moved-out alanlar üzerinden drop
4. Kullanıcı türetilebilir `Copy`/`Clone` protokolleri
5. `defer` ifadesi (şu an yalnızca `deinit`/Drop var)
6. Operator overloading genişletmesi: `[]`, `()`, bitwise/shift, unary neg, compound assignment
7. Bağlamdan literal tip çıkarımıyla genel sayısal coercion

---

## 3. Stdlib ve Ekosistem

### 3.1 Stdlib boşlukları (etki sırasıyla)

1. **Generic `Map<K,V>`** — stdlib'de yalnızca monomorfik HashSet (I64/Str) ve BTreeMap varyantları var — **built-in Map tamamlandı (2026-07: coercion fix, size/isEmpty/clear/keys/values; for (k,v) zaten vardı)**
2. **CLI argüman parser** — `os.getArgs()` ham liste; flag/subcommand/usage çatısı yok — **tamamlandı (2026-07, cli::cli)**
3. **Kripto eksikleri** — sadece hash/HMAC/JWT var; AES, parola hash'leme (argon2/bcrypt/pbkdf2), anahtar üretimi için secure random yok
4. **Math/random tamamlama** — atan2/asin/acos, exp, sinh/cosh/tanh, gcd/lcm, NaN/Inf sınıflandırma; seed'lenebilir RNG, gaussian, shuffle/choice
5. **Timezone desteği** — DateTime aritmetiği sağlam ama saat dilimi/ofset yönetimi yok
6. YAML (TOML var), bağımsız sıkıştırma modülü API'si, logging dosya/rotasyon hedefleri

### 3.2 UI modülü

Geniş widget seti mevcut (23+ widget, menü/toolbar, data binding Faz 6.x). Eksikler: dialog helper'ları (file/color/message), rich-text/HTML view, chart, TreeView/DataGrid için data binding, DataGrid model/sıralama/düzenleme kancaları, liste içi drag-reorder, erişilebilirlik katmanı.

### 3.3 Araçlar

- **LSP** çok kapsamlı (rename, semanticTokens, inlayHint, callHierarchy dahil). Eksikler: `prepareRename`, `typeDefinition`/`implementation`, range formatting, pull diagnostics, `didChangeWatchedFiles`.
- **DAP** ayrı bir AST yorumlayıcısı üzerinde (`DAPInterpreter.cpp`, 1.758 satır) — derlenen kodla davranış sapması riski yapısal; en azından dokümante edilmeli. Function/data breakpoint ve set-variable yok.
- **Paket yöneticisi** tüketici tarafında tam (SemVer `^`/`~`, registry sorgusu, `liva.lock`, transitive çözümleme) ama **publish komutu yok**, indirme checksum doğrulaması yok, çalışan registry sunucusu yok.
- **Playground** statik HTML; WASM backend'i yok.

### 3.4 Örnek/dokümantasyon açığı (düşük maliyet, yüksek getiri)

`examples/` içinde ~55 örnek var ama **http, websocket, json, db/sqlite/postgres, regex, crypto, jwt, csv, toml, time, stream, sync için tek örnek yok** — son aylarda en çok emek verilen stdlib yüzeyi örneksiz — **tamamlandı (2026-07: 11 yeni örnek + ExamplesTest kapısı; examples/README.md)**. Artifact temizliği maddesi başından beri gereksizmiş: `.exe`/`.dll` zaten kök `.gitignore`'da (`*.exe`, `*.dll`, ayrıca fazladan `examples/*.exe`) ve `examples/` altında hiçbir artifact git'e tracked değil (doğrulandı: `git ls-files examples/ | grep -E '\.(exe|dll)$'` boş döner) — yerel wx DLL'leri (`wxbase331u_vc_x64_custom.dll` vb.) UI demo'larının çalışması için gerekli ve olduğu gibi kalmalı. Docs en/tr çift bakım — drift riski; COOKBOOK/TUTORIAL yeni fluent API'lerin (json/http/ws yeniden yazımları) gerisinde kalmış olabilir.

---

## Önerilen Öncelik Sırası

| # | İş | Tür | Etki |
|---|-----|------|------|
| 1 | `visitCallExpr`'ı parçala — **IRGenCall + TypeChecker tamamlandı (2026-07)** | Refactor | Tüm gelecek işlerin hızını artırır |
| 2 | Runtime ABI'yi tek `.def` tablosuna indir — **tamamlandı (2026-07, RuntimeFunctions.def)** | Refactor | 3 yönlü senkron hatası sınıfını yok eder |
| 3 | Yapısal Pattern AST + eksik pattern türleri — **Faz A+B tamamlandı (2026-07)** — kalan: struct destructuring, if-let tam pattern, editör gramerleri (ayrı işler) | Dil | En büyük dil sağlamlığı açığı |
| 4 | Atama/if-let move takibi — **tamamlandı (2026-07, muhafazakâr kapsam: Drop'lu tipler)** — kalan: clone(), atamada eski-değer drop'u, koleksiyon/alan drop'ları | Bug/Dil | Bilinen 3 memory hatasını kökten çözer |
| 5 | `??` operatörünü genel LHS'lerde doğru üret — **tamamlandı (2026-07, + sağ-assoc + lazy RHS)** | Bug | Sessiz yanlış davranış |
| 6 | Generic `Map<K,V>` + CLI arg parser | Stdlib | En görünür kullanıcı boşlukları |
| 7 | Networking/db/json örnekleri + artifact temizliği | Docs | Düşük maliyet, yüksek getiri — **tamamlandı (2026-07)** |
| 8 | CI'da `-Werror` + fixture-skip'leri hard-fail yap — **tamamlandı (2026-07, LIVA_WERROR + fixture hard-fail)** | Altyapı | Regresyon sızıntısını kapatır |
