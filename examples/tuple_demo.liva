func divmod(a: i32, b: i32) -> (i32, i32) {
    return (a / b, a % b)
}

func main() {
    let pair = (42, "hello")
    let x = pair.0
    let y = pair.1
    println(x)
    println(y)

    let (q, r) = divmod(17, 5)
    println(q)
    println(r)
}
