type Int = i32
type Str = string

func add(a: Int, b: Int) -> Int {
    return a + b
}

func main() {
    let x: Int = 10
    let y: Int = 20
    let sum = add(x, y)
    println(sum)

    let name: Str = "Liva"
    println(name)
}
